import{useState}from'react'
import{PublicHeader,PublicLegalLinks,PublicSupportBlock}from'../components/public'
import{useSystemSettings}from'../systemSettings'
type Guidance='qr'|'tracking'|null
export function PublicHome(){const{settings}=useSystemSettings(),[guidance,setGuidance]=useState<Guidance>(null),name=settings?.company_display_name||'MontaGest';return <div className="public-page"><PublicHeader/><main className="public-home"><section className="public-hero"><div className="public-mark" aria-hidden="true">M</div><p className="eyebrow">Bem-vindo</p><h1>{name}</h1><p className="public-lead">Após sua compra, a loja registra a venda e nossa equipe inicia o atendimento para organizar sua montagem.</p><div className="public-home-actions"><button className="secondary" onClick={()=>setGuidance('qr')} aria-expanded={guidance==='qr'}>Já tenho um QR Code</button><button className="secondary" onClick={()=>setGuidance('tracking')} aria-expanded={guidance==='tracking'}>Acompanhar solicitação</button><a className="public-login-action" href="/login">Entrar no sistema</a></div>{guidance&&<div className="public-guidance" role="status"><strong>{guidance==='qr'?'Use o código do seu produto':'Acompanhamento de solicitação'}</strong><p>{guidance==='qr'?'O QR Code continua disponível como opção para produtos que o possuem. Leia-o usando a câmera do celular.':'Em breve você poderá acompanhar sua solicitação por aqui.'}</p></div>}</section><PublicSupportBlock/><PublicLegalLinks/></main></div>}
